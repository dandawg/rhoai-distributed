# Strategic merge patch: only the ray component (Managed). Optional; rhoai-deploy
# base DSC enables Ray by default. Use Argo ServerSideApply on the DSC Application.
apiVersion: datasciencecluster.opendatahub.io/v2
kind: DataScienceCluster
metadata:
  name: default-dsc
spec:
  components:
    ray:
      managementState: Managed
